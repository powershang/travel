# 2026 佐賀旅行 App

親子旅行（2大2小）的行程管理 PWA，部署後用手機瀏覽器開啟即可使用。

---

## 專案概覽

| 項目 | 說明 |
|------|------|
| 旅行日期 | 2026/2/26 (四) ~ 3/3 (二)，6天5夜 |
| 目的地 | 佐賀縣（佐賀市→唐津→伊万里→嬉野→豪斯登堡→鹿島・太良） |
| 航班 | IT246 桃園→佐賀 08:10-11:15 / IT247 佐賀→桃園 11:25-13:05 |
| 人數 | 2 大人 + 2 小孩（3歲、1歲） |
| 預約名 | Wu Tsung Hsuan / +886-981053092 |

---

## 檔案結構

```
saga-trip-app/
├── app.py                 # Flask 伺服器（Render 部署用）
├── requirements.txt       # Python 依賴：flask, gunicorn
├── render.yaml            # Render 自動部署設定
├── README.md              # 本文件
└── static/
    ├── index.html         # PWA 主程式（單一 HTML，所有功能內嵌）
    └── 2026佐賀行程.ics   # iPhone 行事曆匯入檔
```

### 各檔案說明

#### `static/index.html`（PWA 主程式）
- **單一 HTML 檔案**，內嵌 CSS + JavaScript + 所有行程資料
- 地圖使用 [Leaflet.js](https://leafletjs.com/) + Google Maps 圖磚
- 所有 GPS 座標已透過 Tabelog GeoCoordinates / asobo-saga.jp / Mapion 逐一驗證
- 支援 PWA：Service Worker 離線快取、Web App Manifest、iOS standalone 模式

#### `static/2026佐賀行程.ics`（行事曆）
- iCalendar 格式，時區 Asia/Tokyo (JST)
- 每個事件含 GEO 座標、30 分鐘前提醒
- 餐廳事件含電話號碼和預約資訊

#### `app.py`（伺服器）
- 極簡 Flask app，只負責 serve static 檔案
- 讀取環境變數 `PORT`（Render 預設 10000）

---

## App 功能

### Tab 1：📅 行程地圖（首頁）
- **左右分割版面**：左邊行程卡片列表、右邊 Google Maps 地圖
- 手機直式自動變成上下分割（上方地圖 35vh、下方卡片）
- **Day1~Day6 切換**：點上方日期 chip 切換天數
- **自動偵測今天**：旅行期間自動跳到當天行程
- **現在指標**：高亮目前時段的行程卡片 + 下一站倒數
- **地圖↔卡片連動**：
  - 點卡片 → 地圖飛到該點並開 popup
  - 點地圖標記 → 左邊自動滾到對應卡片並藍框高亮
- **每張卡片展開後顯示**：
  - 📍 Google Maps 導航按鈕（iPhone 有裝 Google Maps App 會直接開 App）
  - 📞 一鍵撥打電話（餐廳）
  - ⭐ Tabelog 連結（餐廳）
  - 備註資訊（推薦料理、價格、預約狀態）

### Tab 2：🍽 美食指南
- 11 道特色美食，每道含：
  - 中文名 + **日文名**（大字體，可直接給店員看點餐）
  - 價格、推薦指數、在哪吃
- 季節海鮮指南：
  - 2/27 唐津烏賊季（ヤリイカ、アオリイカ、コウイカ）
  - 3/2 有明海（竹崎牡蠣、竹崎蟹、ムツゴロウ）

### Tab 3：📱 餐廳預約
- 6 間餐廳完整預約資訊
- 一鍵撥打電話 / Google Maps 導航 / Tabelog 連結
- **手動標記「已確認/待確認」**（狀態存在 localStorage，關掉再開不會消失）

---

## 餐廳預約一覽

| # | 餐廳 | 日期 | 時間 | 電話 | 料理 |
|---|------|------|------|------|------|
| 1 | 佐賀牛レストラン季楽 本店 | 2/26 (四) | 18:00 | 0952-28-4132 | 佐賀牛鐵板燒 |
| 2 | いかの活造り 大和 | 2/27 (五) | 18:00 | 0955-82-3643 | 活烏賊刺身 |
| 3 | 鷹鮨 | 2/28 (六) | 18:00 | 0954-43-3210 | 壽司/海鮮 |
| 4 | PIZZERIA MONTE STELLA | 3/1 (日) | 18:00 | 0954-20-2020 | 窯烤披薩 |
| 5 | 川した | 3/2 (一) | **12:00** | 0954-68-2645 | 竹崎蟹（午餐） |
| 6 | 寿し政 | 3/2 (一) | 18:00 | 0954-62-5617 | 有明海壽司 |

> 預約名：Wu Tsung Hsuan / 2大2小(3歲、1歲) / 禁菸

---

## 住宿一覽

| 日期 | 住宿 | 地區 |
|------|------|------|
| 2/26 | airbnb 若宮。薰風 | 佐賀市 |
| 2/27 | Riverside Hotel 唐津城 | 唐津市 |
| 2/28~3/1 | Hotel Sakura 嬉野（2晚） | 嬉野溫泉 |
| 3/2 | airbnb 鹿島 | 鹿島市 |

---

## 部署方式（Render）

### 前置作業
1. 註冊 [GitHub](https://github.com) 帳號（如果還沒有）
2. 註冊 [Render](https://render.com) 帳號（可用 GitHub 登入）

### 步驟

```bash
# 1. 進入專案資料夾
cd C:\Users\shane\Downloads\saga-trip-app

# 2. 初始化 Git
git init
git add .
git commit -m "2026 saga trip app"

# 3. 在 GitHub 建立新 repo（名稱例如 saga-trip-app）
#    然後推上去
git remote add origin https://github.com/<你的帳號>/saga-trip-app.git
git branch -M main
git push -u origin main
```

4. 到 [Render Dashboard](https://dashboard.render.com/) → **New +** → **Web Service**
5. 選擇 **Connect a repository** → 選你剛建的 repo
6. Render 會自動偵測 `render.yaml` 設定：
   - Runtime: Python
   - Build: `pip install -r requirements.txt`
   - Start: `gunicorn app:app`
   - Plan: Free
7. 點 **Create Web Service** → 等待部署完成（約 1-2 分鐘）
8. 部署完成後取得網址，例如：`https://saga-trip-app.onrender.com`

### 部署後使用
- **手機瀏覽器**：直接開啟網址即可看到完整 App
- **加入主畫面**：Safari 開啟 → 點分享 ⬆️ → 「加入主畫面」→ 主畫面出現 App 圖示
- **行事曆下載**：訪問 `https://<你的網址>/2026佐賀行程.ics` → 自動匯入 iPhone 行事曆

---

## 本地開發 / 測試

```bash
# 安裝依賴
pip install flask

# 啟動伺服器
python app.py

# 瀏覽器開啟
# http://localhost:10000
```

手機測試（同 WiFi）：
```bash
# 查看電腦 IP
ipconfig  # Windows
ifconfig  # Mac

# 手機 Safari 開啟
# http://192.168.x.x:10000
```

---

## 技術細節

| 技術 | 用途 |
|------|------|
| HTML/CSS/JS | 單頁應用，全部內嵌在 index.html |
| Leaflet.js 1.9.4 | 地圖渲染引擎（CDN 載入） |
| Google Maps Tiles | 地圖圖磚（`mt1.google.com`） |
| Service Worker | 離線快取（Blob URL 註冊） |
| Web App Manifest | PWA 安裝支援（Blob URL 注入） |
| localStorage | 儲存餐廳預約確認狀態 |
| Flask + Gunicorn | 伺服器（Render 部署） |
| iCalendar (RFC 5545) | .ics 行事曆檔案 |

### GPS 座標來源
所有 27 個地點座標已透過以下來源逐一驗證：
- Tabelog 頁面內嵌 schema.org GeoCoordinates
- asobo-saga.jp 佐賀縣官方觀光入口網站
- Mapion.co.jp POI 資料庫
- geocoding.jp 地址轉座標

### 響應式設計
- **寬螢幕**（>540px 或橫式）：左邊 45% 卡片 + 右邊 55% 地圖
- **窄螢幕**（≤540px 直式）：上方 35vh 地圖 + 下方卡片列表
- iOS safe area 支援（瀏海 / Home Indicator）

---

## 修改行程資料

所有行程資料都在 `static/index.html` 的 `<script>` 區塊內，以 JavaScript 物件格式儲存：

- **`itinerary`** — 6 天行程（時間、地點名、座標、備註、電話、Tabelog 連結）
- **`foods`** — 美食清單（中文名、日文名、價格、推薦、說明）
- **`reservations`** — 餐廳預約（電話、Tabelog、人數、備註、座標）

修改後重新 `git push`，Render 會自動重新部署。

---

## 注意事項

- **Render Free Plan** 會在 15 分鐘無流量後休眠，首次訪問需等約 30 秒喚醒
- Google Maps 圖磚是非官方 URL（`mt1.google.com`），大量商用可能被限制，個人旅行使用沒問題
- Service Worker 以 Blob URL 註冊，在某些瀏覽器的嚴格模式下可能無法運作；透過 Render HTTPS 部署後則完全正常
- `.ics` 檔案的提醒時間都是 30 分鐘前，最後一晚（3/2）有額外 10 小時前提醒（提醒早起去機場）
